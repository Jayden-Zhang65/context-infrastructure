# 2026-07-24 每日记录

## 项目概览

今日在 5 个会话中工作，覆盖 4 个项目目录。

| 项目 | 会话 | 消息量 |
|------|------|--------|
| LIMS | LIMS 审计批量修改检测阈值优化 | 356 |
| Hermes | OpenCode 集成 context-infrastructure 架构 | 75 |
| Ops | Windows 10 共享文件夹主机名改 IP + 局域网分发 | 43 |
| QMS | CentOS 下查找 Nginx 重启命令路径 | 9 |
| Ops | T_QMS_SUPPLIER 修改审计日志查询 | 2 |

## 主要活动

### 1. LIMS — 审计批量修改检测阈值优化
最重的会话（356 条消息）。围绕 LIMS 系统的审计相关功能做了：
- 批量修改检测阈值逻辑调整
- 涉及多个 check 函数（check_09, check_10, check_11）的重写和修复
- 使用了 @general 和 @explore subagent 进行代码探索和重构
- SQL 查询中增加了 end_date 字段支持

### 2. Hermes — OpenCode 知识库架构集成
将 context-infrastructure（rules/skills/axioms/contexts）接入 OpenCode 全局配置：
- 创建 `~/.config/opencode/AGENTS.md`（全局指令，跨项目生效）
- 更新 `opencode.jsonc`：instructions（自动加载 SOUL/USER/COMMUNICATION/WORKSPACE）+ references（@context-infra）
- 27 个 OpenCode SKILL.md 包装器（懒加载模式）
- 创建 `context-infrastructure/CLAUDE.md`（Claude Code/Kiro 兼容）
- 诊断并修复知识飞轮断点：创建 workflow_daily_review.md、/daily-review 命令
- 更新 opencode_job.py 默认模型为 deepseek-v4-flash

### 3. Ops — Windows 共享文件夹网络配置
- 将共享文件夹从主机名访问改为 IP 地址访问
- 分发到局域网
- T_QMS_SUPPLIER 表的审计日志查询

### 4. QMS — CentOS Nginx 运维
- 查找并执行 Nginx 重启命令

## 关键发现

- **OpenCode session 数据库可查**：`~/.local/share/opencode/opencode.db` 是 SQLite 数据库，包含 session、message、session_input 等表，可以跨项目查询当日所有对话。每日复盘可以用它自动化——不依赖 agent 记忆。
- **SKILL.md 包装器模式**让 context-infrastructure 的 skill 文件可以被 OpenCode 原生发现，不需要全量注入。27 个包装器每个 ~6 行代码。
- **知识飞轮断点**根因：PC 端 CLAUDE.md/AGENTS.md 缺少 Knowledge Capture 段落，agent 不会主动写 daily_records。已修复。

## 新 Skill

- **opencode-session-query** — 从 OpenCode session 数据库跨项目查询对话历史。脚本 `tools/query_opencode_sessions.py` + SKILL.md 包装器 + `rules/skills/opencode_session_query.md`。已集成到 workflow_daily_review.md Phase 1。

## 待跟进

- [ ] 安装 Git for Windows，打通 PC ↔ 服务器同步管道
- [ ] 配置 Windows Task Scheduler 每日定时自动复盘（用 opencode_job.py + session DB 查询）
- [ ] 重启 OpenCode 验证新 skill 和 /daily-review 命令加载成功
- [ ] 确认 Hermes 端 daily-summary cron 是否需要恢复文件读取（目前只查 session_search）
